spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
#oauth:
#  github:
#    client:
#      id: {GITHUB_CLIENT_ID}
#      secret: {GITHUB_CLIENT_SECRET}